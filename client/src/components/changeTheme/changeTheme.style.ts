import styled from "styled-components";

const ChangeThemeWrapper = styled.div`
  display: flex;
  align-items: center;
  button {
    margin: 0 5px;
    border-radius: 50%;
    min-width: 20px;
    min-height: 20px;
  }
`;

export default ChangeThemeWrapper;
